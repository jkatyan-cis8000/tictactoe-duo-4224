# src/config

Contains configuration constants and settings.

This layer should contain:
- Hard-coded constants (board size, defaults)
- Environment variable wrappers
- Feature flag definitions

May import from: types, config
