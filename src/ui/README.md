# src/ui

Contains user-facing surfaces - CLI, web, GUI.

This layer should contain:
- Input handling
- Display/rendering logic
- User interaction flows

May import from: types, config, service, runtime, providers, ui
